import styled from "styled-components";

const CharacterCardElement = styled.div`
    display: grid;
    grid-gap: 1em;
    grid-template-columns: auto 1fr;
    grid-template-rows: auto auto auto;
    border: solid 2px white;
    padding: 1em;
    border-radius: 20px;
`;

export default CharacterCardElement;
